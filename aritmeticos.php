<?php
      echo "Hello !";// mostra a tela

      // variaveis
      // toda variavel em php inicia com $ (cifrão)
      $a = 10;
      $b = 20;

      // operadores de atribuição =

      $soma = $a + $b;

      echo "<br>" . $soma; //HTML concatenado com variavel PHP
      /* 
      $a + $b	Adição	Soma de $a e $b.
      $a - $b	Subtração	Diferença entre $a e $b.
      $a * $b	Multiplicação	Produto de $a e $b.
      $a / $b	Divisão	Quociente de $a e $b.
      $a % $b	Módulo	Resto de $a dividido por $b.
      $a ** $b	Exponencial	Resultado de $a elevado a $b.
           Exemplos

           */
       $sub = $a - $b;
       echo "<br>Subtração: ".$sub;  //-10

       $mult = $a * $b;
       echo "<br>Multiplicação: ".$mult;  // 200

       $div = $a / $b;
       echo "<br>Divisão: ".$div;  // 0.5

       $modulo = $a % $b;
       echo "<br>Subtração: ".$modulo;  //10
       
       $exp = $a ** 3;
       echo "<br> Expoenciação: ".$exp;  //1000
         
      
      
      
      // desvio condicional

      // laço de repetição

      // 

?>
